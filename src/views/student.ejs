<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Student Result</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Montserrat", sans-serif;
      }
      body {
        min-height: 100vh;
        background: #f8f8f8;
      }
      .header {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #642c61 60%, #2e272b);
        color: white;
        padding: 2rem;
        position: relative;
        overflow: hidden;
      }
      .header::after {
        content: "";
        position: absolute;
        bottom: 0;
        right: 0;
        width: 100%;
        height: 5px;
        background: linear-gradient(90deg, #642c61, #2e272b);
      }
      .institution-name {
        font-size: 32px;
        font-weight: 700;
        margin-bottom: 5px;
        text-transform: uppercase;
        letter-spacing: 2px;
      }
      .institution-quote {
        font-size: 16px;
        color: rgba(255, 255, 255, 0.9);
        font-weight: 500;
        letter-spacing: 3px;
        margin-bottom: 10px;
        font-style: italic;
      }
      .portal-name {
        font-size: 18px;
        color: rgba(255, 255, 255, 0.9);
        font-weight: 500;
        margin-top: 5px;
      }
      .container {
        max-width: 800px;
        margin: 40px auto;
        padding: 40px;
        background: white;
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(100, 44, 97, 0.1);
        position: relative;
        overflow: hidden;
      }
      .container::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 5px;
        background: linear-gradient(90deg, #642c61, #2e272b);
      }
      .error {
        color: #721c24;
        background: linear-gradient(135deg, #ffebee 0%, #fff 100%);
        padding: 20px;
        border-radius: 12px;
        text-align: center;
        margin-bottom: 20px;
        border: 1px solid rgba(114, 28, 36, 0.1);
        font-weight: 500;
      }
      h2 {
        color: #642c61;
        margin-bottom: 30px;
        text-align: center;
        font-size: 24px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        position: relative;
        padding-bottom: 15px;
      }
      h2::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 100px;
        height: 3px;
        background: linear-gradient(90deg, #642c61, #2e272b);
      }
      .result-table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0 10px;
        margin-top: 20px;
      }
      .result-table tr {
        transition: transform 0.3s ease;
      }
      .result-table tr:hover {
        transform: translateX(10px);
      }
      .result-table td {
        padding: 20px;
        border-radius: 8px;
      }
      .result-label {
        font-weight: 600;
        color: #642c61;
        width: 200px;
        background: linear-gradient(135deg, #f8f8f8 0%, #fff 100%);
        text-transform: uppercase;
        font-size: 14px;
        letter-spacing: 0.5px;
      }
      .result-value {
        color: #2e272b;
        background: white;
        font-weight: 500;
      }
      .result-value.rank-value {
        font-size: 24px;
        font-weight: 700;
        color: #642c61;
        text-align: center;
        background: linear-gradient(135deg, #f8f8f8 0%, #fff 100%);
      }
      .medium-badge {
        display: inline-block;
        padding: 5px 12px;
        border-radius: 4px;
        background: #642c61;
        color: white;
        font-size: 12px;
        letter-spacing: 1px;
      }
      .back-link {
        display: inline-block;
        margin-top: 30px;
        padding: 12px 25px;
        background: linear-gradient(135deg, #642c61 0%, #2e272b 100%);
        color: white;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 600;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-size: 14px;
      }
      .back-link:hover {
        transform: translateX(-5px);
        box-shadow: 0 5px 15px rgba(100, 44, 97, 0.3);
      }
      .footer {
        text-align: center;
        padding: 20px;
        color: #2e272b;
        font-size: 14px;
        position: fixed;
        bottom: 0;
        width: 100%;
        background: white;
        border-top: 1px solid rgba(46, 39, 43, 0.1);
      }
      @media print {
        .back-link,
        .footer {
          display: none;
        }
        .container {
          box-shadow: none;
          margin: 0;
          padding: 20px;
        }
        body {
          background: white;
        }
      }
      .scholarship-badge {
        display: inline-block;
        padding: 8px 16px;
        border-radius: 8px;
        font-weight: 600;
        font-size: 14px;
        letter-spacing: 1px;
      }
      .scholarship-100 {
        background: #28a745;
        color: white;
      }
      .scholarship-50 {
        background: #17a2b8;
        color: white;
      }
      .scholarship-25 {
        background: #ffc107;
        color: #2e272b;
      }
      .scholarship-not {
        background: #dc3545;
        color: white;
      }
      .scholarship-label {
        color: #642c61;
        font-size: 18px;
        font-weight: 700;
      }
      .scholarship-row {
        background: linear-gradient(135deg, #f8f8f8 0%, #fff 100%);
        border: 2px solid #642c61;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <div class="institution-name">TRACE</div>
      <div class="institution-quote">MANIFEST YOUR DESIRES</div>
      <div class="portal-name">Student Result Portal</div>
    </div>

    <div class="container">
      <% if (error) { %>
      <div class="error"><%= error %></div>
      <a href="/" class="back-link">← Back to Search</a>
      <% } else { %>
      <h2>Student Result Details</h2>
      <table class="result-table">
        <tr>
          <td class="result-label">Rank</td>
          <td class="result-value rank-value">#<%= student.rank %></td>
        </tr>
        <tr class="scholarship-row">
          <td class="result-label scholarship-label">Scholarship</td>
          <td class="result-value">
            <span
              class="scholarship-badge scholarship-<%= student.scholarship === 'Not Eligible' ? 'not' : student.scholarship %>"
            >
              <%= student.scholarship === 'Not Eligible' ? 'Not Eligible' :
              `${student.scholarship}% Scholarship` %>
            </span>
          </td>
        </tr>
        <tr>
          <td class="result-label">Register Number</td>
          <td class="result-value"><%= student.registerNumber %></td>
        </tr>
        <tr>
          <td class="result-label">Student Name</td>
          <td class="result-value"><%= student.name %></td>
        </tr>
        <tr>
          <td class="result-label">Institution</td>
          <td class="result-value"><%= student.institution %></td>
        </tr>
        <tr>
          <td class="result-label">Medium</td>
          <td class="result-value">
            <span class="medium-badge"><%= student.medium %></span>
          </td>
        </tr>
        <tr>
          <td class="result-label">Mark</td>
          <td class="result-value"><%= student.mark %>/100</td>
        </tr>
      </table>
      <a href="/" class="back-link">← Back to Search</a>
      <% } %>
    </div>

    <div class="footer">© 2025 Trace. All rights reserved.</div>
  </body>
</html>
